# Etapa 06 — Múltiplos botões

## Objetivo

Usar `querySelectorAll` e `forEach` para aplicar o mesmo comportamento a vários elementos.

## O que observar

Abra os arquivos desta pasta, execute o `index.html` no navegador e compare com a etapa anterior.

## Commit sugerido

```bash
git add .
git commit -m "Etapa 06: múltiplos botões"
```
