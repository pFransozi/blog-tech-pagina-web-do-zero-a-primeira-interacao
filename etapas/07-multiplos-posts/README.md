# Etapa 07 — Múltiplos posts

## Objetivo

Agrupar cada post em `article` e evitar que o layout de um post afete os demais.

## O que observar

Abra os arquivos desta pasta, execute o `index.html` no navegador e compare com a etapa anterior.

## Commit sugerido

```bash
git add .
git commit -m "Etapa 07: múltiplos posts"
```
